THREE BUGS TO FIX:

════════════════════════════════════════════════════════
BUG 1 — Adhan audio preview broken on iOS production
════════════════════════════════════════════════════════

The adhan preview works in Expo Go (Android) but 
fails in the iOS TestFlight production build.

Investigate and fix the audio session configuration 
in the adhan preview component:

1. Find where the adhan preview is implemented:
   grep -rn "preview\|adhan\|Audio\|Sound" \
     app/ components/ lib/ \
     --include="*.tsx" --include="*.ts" | \
     grep -i "preview\|adhan" | head -20

2. Ensure expo-av Sound is properly configured for 
   iOS production with:
   await Audio.setAudioModeAsync({
     playsInSilentModeIOS: true,
     staysActiveInBackground: false,
   });

3. Ensure Sound object is properly unloaded after 
   preview stops to prevent crashes.

════════════════════════════════════════════════════════
BUG 2 — Settings tab: move to bottom bar
════════════════════════════════════════════════════════

Move the Settings button from its current location 
to the bottom tab bar with these requirements:

1. Always the LAST tab, far right — fixed position 
   regardless of RTL/LTR language direction.

2. Larger gap between Settings tab and the last 
   main tab — use marginLeft or a spacer to create 
   ~16-24px extra separation.

3. Color: Settings tab icon and label use BLUE 
   when active (not the main tabs' green).
   Use a theme-safe blue: #4A90D9 or similar steel 
   blue that works across all app themes (light, 
   dark, green-tinted).
   When inactive: same grey as other inactive tabs.

4. The tab should still highlight/animate when 
   selected — just in blue instead of green.

════════════════════════════════════════════════════════
BUG 3 — Quran Surah list: wrong language for 
         secondary Surah names
════════════════════════════════════════════════════════

In the Quran tab, the Surah list shows two lines 
per Surah — the primary name and a secondary name. 
For some of the 14 newly bundled languages, the 
secondary name is showing in English instead of 
the selected language.

1. Find where Surah names are rendered:
   grep -rn "surahName\|surah_name\|SurahName\|transliteration" \
     app/ components/ constants/ \
     --include="*.tsx" --include="*.ts" | head -20

2. Check if the bundled translation JSON files 
   include Surah name translations or only verse text.

3. If Surah names are missing from the JSON files, 
   add a Surah name lookup that uses the correct 
   language — fall back to Arabic (not English) 
   if the translation is unavailable.

Report findings for each bug before making changes.